import React from "react";
import { SectionContainer } from "./CommentSection.styles";

const CommentSection: React.FC = () => (
  <SectionContainer>COMMENT SECTION</SectionContainer>
);

export default CommentSection;
